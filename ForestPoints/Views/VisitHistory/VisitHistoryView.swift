import SwiftUI

struct VisitHistoryView: View {
    var body: some View {
        VStack(spacing: 0) {
            HeaderView(title: "Visit History")
            
            Spacer()
        }
    }
}

