import SwiftUI

struct CollectionsView: View {
    var body: some View {
        VStack(spacing: 0) {
            HeaderView(title: "Forest Points\nCatalog")
            
            Spacer()
        }
    }
}

